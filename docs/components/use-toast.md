# Toast Hook

The `use-toast` hook provides a way to show toast notifications in the UI.

- **Location:** hooks/use-toast.ts
- **Usage:**
```
const { showToast } = useToast();
showToast('Message!');
```
- **Tech:** React
