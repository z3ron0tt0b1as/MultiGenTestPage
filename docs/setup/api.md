# API Endpoints

This project exposes several REST API endpoints for authentication and project management.

## Auth Endpoints
- `/api/auth/login` - User login
- `/api/auth/register` - User registration
- `/api/auth/verify` - Email verification

## Usage
All endpoints accept and return JSON. See the source files in `app/api/auth/` for details.
