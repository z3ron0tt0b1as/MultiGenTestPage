# Environment Variables

- `DATABASE_URL`: MySQL connection string
- Add other environment variables as needed for authentication, API keys, etc.

Example:
```
DATABASE_URL="mysql://USER:PASSWORD@localhost:3306/DATABASE"
```
